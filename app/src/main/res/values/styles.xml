<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--dialog动画-->
    <style name="view_slide_anim" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/view_slide_in_bottom</item>
        <item name="android:windowExitAnimation">@anim/view_slide_out_bottom</item>
    </style>

    <!--带输入框-->
    <style name="InputDialog" parent="@android:style/Theme.Dialog">
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 遮罩层 -->
        <item name="android:backgroundDimAmount">0.5</item>
        <!--        <item name="android:windowAnimationStyle">@style/view_scale_anim</item>-->
        <!--显示软件盘-->
        <item name="android:windowSoftInputMode">stateAlwaysHidden</item>
    </style>
</resources>
